const state = {
    data : 2
};

const getters = {
    DataSet : (state) => state.data
};

const actions = {};

const mutations = {};

export default {
    state,
    getters,
    actions,
    mutations
}